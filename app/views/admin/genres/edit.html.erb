<h1>管理者</h1>
<h2>ジャンル編集</h2>

<%= form_with model:@genre, url: admin_genre_path, local: true do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <h4>有効ステータス</h4>
  <%= f.select :is_active, [["有効", "TRUE"], ["無効", "FALSE"]] %>
  <br>
  <%= f.submit "送信" %>
<% end %>